export const metadata = {
  title: 'Sign Up - SNSF',
  description: 'Sign up page for SNSF',
}

export default function SignupLayout({ children }) {
  return (
    <div className="font-sans">
      {children}
    </div>
  );
}
